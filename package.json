{
  "name": "spotifyproject",
  "version": "1.0.0",
  "description": "The following dependencies are needed: <br />\r spotipy - ```pip3 install spotipy``` <br />\r dotenv - ```pip3 install python-dotenv```",
  "dependencies": {
    "@testing-library/jest-dom": "^5.17.0",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "main": "index.js",
  "scripts": {
    "setup": "npm install && cd client && npm install",
    "start": "concurrently \"npm --prefix client start\" \"uvicorn spotify_project.api.controller:app --reload\"",
    "start-fastapi": "uvicorn spotify_project.api.controller:app --reload",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
  "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

